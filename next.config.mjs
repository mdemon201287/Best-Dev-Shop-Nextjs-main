/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['example.com'], // Add external domains if needed
    },
    // Other Next.js configurations can go here
  };
  
  export default nextConfig;
  